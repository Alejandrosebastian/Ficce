@*@model IEnumerable<FICCE.Models.Edificio>

    @{
        ViewData["Title"] = "Index";
    }

    <h2>Index</h2>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Nombre)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Ubicacion)
                    </th>
                <th>
                    @Html.DisplayNameFor(model => model.Evento)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Ubicacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Evento.EventoId)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.EdificioId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.EdificioId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.EdificioId">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>*@

@{
    ViewData["Title"] = "Planta";
}

<h2>Lista de planta</h2>

<p>
    <a href="#" data-toggle="modal" data-target="#IngresoPlanta">Ingreso Planta</a>
</p>
<table class="table">
    <thead>
        <tr>

            <th>Nombre</th>
            <th>
                Edificio
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody id="ListaPlanta"></tbody>
</table>







<div class="modal fade" id="IngresoPlanta" tabindex="-1"
     role="dialog" aria-labelledby="EtiquetaModal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close"
                        data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
                <h4 class="modal-title"
                    id="EtiquetaModal">
                    Plantas
                </h4>
            </div>
            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label class="control-label">Evento</label>
                        <input id="edificio" name="edificio" class="form-control" placeholder="ingrese el id del edificio" />
                    </div>
                    <div class="form-group">
                        <label class="control-label">Nombre</label>
                        <input id="nombre" name="nombre" class="form-control" placeholder="ingrese el nombre de la planta" />
                    </div>
                    <input id="id" name="id" hidden />
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cerrar</button>
                <button type="button" class="btn btn-primary" data-toggle="tooltip"
                        title="con este boton usted guradara un nuevo cliente" onclick="grabaPlanta()">
                    Guardar
                </button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="ImpresionPlanta" tabindex="-1"
     role="dialog" aria-labelledby="EtiquetaModal">
    <div class="modal-dialog" role="document" id="areaImprimir">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close"
                        data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
                <h4 class="modal-title"
                    id="EtiquetaModal">
                    Reportes de Plantas
                </h4>
            </div>
            <div class="modal-body">
                <div>
                    <table class="table">
                        <thead>
                            <tr class="danger">
                                <td>Id Planta</td>
                                <td>ID Edificios</td>
                                <td>Nombre</td>
                            </tr>
                        </thead>
                        <tbody id="CuerpoImpresionPlanta"> </tbody>
                    </table>

                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cerrar</button>
                <button type="button" class="btn btn-primary" data-toggle="tooltip"
                        title="con este boton usted guradara un nuevo cliente" onclick="Impresion()">
                    Imprimir
                </button>
            </div>
        </div>
    </div>
</div>
